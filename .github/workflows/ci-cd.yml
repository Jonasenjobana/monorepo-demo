name: Deploy to GitHub Pages

on:
  push:
    branches: [master]  # 主分支推送触发

jobs:
  build-deploy:
    runs-on: ubuntu-latest
    permissions:
      pages: write
      id-token: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'pnpm'  # 缓存pnpm依赖

      - name: Prepare pnpm（Node.js 22内置corepack）
        run: corepack prepare pnpm@latest --activate  # 安装并激活pnpm（可指定版本如pnpm@9.0.0）

      - name: Install dependencies
        run: pnpm install --frozen-lockfile  # 锁定依赖版本，更稳定

      - name: Build for GitHub Pages
        run: pnpm run build:github

      - name: Setup Pages
        uses: actions/configure-pages@v4

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: './packages/pkg-1/dist'

      - name: Deploy to GitHub Pages
        uses: actions/deploy-pages@v4